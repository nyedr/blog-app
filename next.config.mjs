/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: { domains: ["images.unsplash.com", "i.postimg.cc", "lh3.googleusercontent.com", "t4.ftcdn.net"], formats: ['image/avif', 'image/webp'], },
  }
  
  export default nextConfig